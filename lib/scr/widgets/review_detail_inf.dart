import 'package:flutter/material.dart';

class ReviewDetailInf extends StatelessWidget {
  const ReviewDetailInf({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('REVIEW', style: TextStyle(color: Colors.black)),
    );
  }
}
